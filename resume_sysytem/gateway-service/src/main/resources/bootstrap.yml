spring:
  application:
    name: gateway-service
  profiles:
    active: dev
  # Nacos服务注册发现
  cloud:
    nacos:
      config:
        enabled: false  # 关闭Nacos配置中心，防止覆盖本地路由配置
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: d0e61529-b169-481a-8e64-6c652f5729c8


