server:
  port: 8087

spring:
  application:
    name: ai-service

ai:
  deepseek:
    api-url: https://api.deepseek.com/v1/chat/completions
    api-key: sk-5d173fbaef3143d28394aedc571c1c61
    timeout: 30s

# Feign配置
feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 10000
  httpclient:
    enabled: true

# JWT配置（如需）
# jwt:
#   secret: xxx
#   expire: 86400 